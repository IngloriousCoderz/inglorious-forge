import { Meta } from '@storybook/addon-docs/blocks'

import { isFunction } from '@inglorious/utils/functions/function'
import { toString as bToString } from '@inglorious/utils/data-structures/boolean'

<Meta title="Utils/Functions/Function" />

# Function

Functions that deal with a single function.

## Is Function

Checks if a value is a function.

<code>isFunction(() => null) == {bToString(isFunction(() => null))}</code>&nbsp;

<code>isFunction(null) == {bToString(isFunction(null))}</code>&nbsp;
